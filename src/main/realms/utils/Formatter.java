package main.realms.utils;

import main.realms.java.Human.Human;

public class Formatter {

    public static String[] formatHuman(Human human) {
        String[] array = null;

        // todo split off formatting from the command itself

        return array;
    }
}
